<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.bit.practice.repository.mapper.StockInfoMapper">

    <insert id="save" parameterType="edu.bit.practice.repository.dao.StockInfo">
        INSERT INTO stock_info (stock_name, start_Price, end_price)
        VALUES (
        #{stockName, jdbcType=VARCHAR},
        #{startPrice, jdbcType=VARCHAR},
        #{endPrice, jdbcType=VARCHAR}
        )
    </insert>


</mapper>